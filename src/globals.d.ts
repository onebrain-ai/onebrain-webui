// Compile-time constants injected by Vite's `define` (see vite.config.ts).

/** The WebUI package version (from package.json), baked in at build time. */
declare const __APP_VERSION__: string;

/** The canonical repository URL (from package.json `homepage`), baked in at build
 *  time — surfaced as links in Settings → About. */
declare const __APP_REPO__: string;
